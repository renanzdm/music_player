import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:musicplayer/app/modules/songs/songs_page.dart';

main() {
  testWidgets('SongsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SongsPage(title: 'Songs')));
    //  final titleFinder = find.text('Songs');
    //  expect(titleFinder, findsOneWidget);
  });
}
