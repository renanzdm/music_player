import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:musicplayer/app/modules/songs/songs_controller.dart';
import 'package:musicplayer/app/modules/songs/songs_module.dart';

void main() {
  initModule(SongsModule());
  // SongsController songs;
  //
  setUp(() {
    //     songs = SongsModule.to.get<SongsController>();
  });

  group('SongsController Test', () {
    //   test("First Test", () {
    //     expect(songs, isInstanceOf<SongsController>());
    //   });

    //   test("Set Value", () {
    //     expect(songs.value, equals(0));
    //     songs.increment();
    //     expect(songs.value, equals(1));
    //   });
  });
}
