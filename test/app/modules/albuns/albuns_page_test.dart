import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:musicplayer/app/modules/albuns/albuns_page.dart';

main() {
  testWidgets('AlbunsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AlbunsPage(title: 'Albuns')));
    //  final titleFinder = find.text('Albuns');
    //  expect(titleFinder, findsOneWidget);
  });
}
