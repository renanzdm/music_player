import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:musicplayer/app/modules/genre/genre_page.dart';

main() {
  testWidgets('GenrePage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(GenrePage(title: 'Genre')));
    //  final titleFinder = find.text('Genre');
    //  expect(titleFinder, findsOneWidget);
  });
}
