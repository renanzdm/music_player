import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:musicplayer/app/modules/genre/genre_controller.dart';
import 'package:musicplayer/app/modules/genre/genre_module.dart';

void main() {
  initModule(GenreModule());
  // GenreController genre;
  //
  setUp(() {
    //     genre = GenreModule.to.get<GenreController>();
  });

  group('GenreController Test', () {
    //   test("First Test", () {
    //     expect(genre, isInstanceOf<GenreController>());
    //   });

    //   test("Set Value", () {
    //     expect(genre.value, equals(0));
    //     genre.increment();
    //     expect(genre.value, equals(1));
    //   });
  });
}
