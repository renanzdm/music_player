import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:musicplayer/app/modules/music/music_page.dart';

main() {
  testWidgets('MusicPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MusicPage(title: 'Music')));
    //  final titleFinder = find.text('Music');
    //  expect(titleFinder, findsOneWidget);
  });
}
