import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:musicplayer/app/modules/reproduction/reproduction_page.dart';

main() {
  testWidgets('ReproductionPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ReproductionPage(title: 'Reproduction')));
    //  final titleFinder = find.text('Reproduction');
    //  expect(titleFinder, findsOneWidget);
  });
}
