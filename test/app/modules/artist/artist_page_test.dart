import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:musicplayer/app/modules/artist/artist_page.dart';

main() {
  testWidgets('ArtistPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ArtistPage(title: 'Artist')));
    //  final titleFinder = find.text('Artist');
    //  expect(titleFinder, findsOneWidget);
  });
}
