import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:musicplayer/app/modules/list_musics/list_musics_page.dart';

main() {
  testWidgets('ListMusicsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ListMusicsPage(title: 'ListMusics')));
    //  final titleFinder = find.text('ListMusics');
    //  expect(titleFinder, findsOneWidget);
  });
}
