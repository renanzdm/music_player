import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:musicplayer/app/modules/list_music/list_music_page.dart';

main() {
  testWidgets('ListMusicPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ListMusicPage(title: 'ListMusic')));
    //  final titleFinder = find.text('ListMusic');
    //  expect(titleFinder, findsOneWidget);
  });
}
