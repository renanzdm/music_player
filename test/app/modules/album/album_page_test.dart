import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:musicplayer/app/modules/album/album_page.dart';

main() {
  testWidgets('AlbumPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AlbumPage(title: 'Album')));
    //  final titleFinder = find.text('Album');
    //  expect(titleFinder, findsOneWidget);
  });
}
