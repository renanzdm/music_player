import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:musicplayer/app/shared/widget/playlist/playlist_controller.dart';
import 'package:musicplayer/app/app_module.dart';

void main() {
  initModule(AppModule());
  // PlaylistController playlist;
  //
  setUp(() {
    //     playlist = AppModule.to.get<PlaylistController>();
  });

  group('PlaylistController Test', () {
    //   test("First Test", () {
    //     expect(playlist, isInstanceOf<PlaylistController>());
    //   });

    //   test("Set Value", () {
    //     expect(playlist.value, equals(0));
    //     playlist.increment();
    //     expect(playlist.value, equals(1));
    //   });
  });
}
