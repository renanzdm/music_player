import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:musicplayer/app/shared/widgets/album_card/album_card_controller.dart';
import 'package:musicplayer/app/app_module.dart';

void main() {
  initModule(AppModule());
  // AlbumCardController albumcard;
  //
  setUp(() {
    //     albumcard = AppModule.to.get<AlbumCardController>();
  });

  group('AlbumCardController Test', () {
    //   test("First Test", () {
    //     expect(albumcard, isInstanceOf<AlbumCardController>());
    //   });

    //   test("Set Value", () {
    //     expect(albumcard.value, equals(0));
    //     albumcard.increment();
    //     expect(albumcard.value, equals(1));
    //   });
  });
}
