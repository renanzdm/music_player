import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:musicplayer/app/shared/widgets/app_bar/app_bar_controller.dart';
import 'package:musicplayer/app/app_module.dart';

void main() {
  initModule(AppModule());
  // AppBarController appbar;
  //
  setUp(() {
    //     appbar = AppModule.to.get<AppBarController>();
  });

  group('AppBarController Test', () {
    //   test("First Test", () {
    //     expect(appbar, isInstanceOf<AppBarController>());
    //   });

    //   test("Set Value", () {
    //     expect(appbar.value, equals(0));
    //     appbar.increment();
    //     expect(appbar.value, equals(1));
    //   });
  });
}
