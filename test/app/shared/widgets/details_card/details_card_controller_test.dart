import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:musicplayer/app/shared/widgets/details_card/details_card_controller.dart';
import 'package:musicplayer/app/app_module.dart';

void main() {
  initModule(AppModule());
  // DetailsCardController detailscard;
  //
  setUp(() {
    //     detailscard = AppModule.to.get<DetailsCardController>();
  });

  group('DetailsCardController Test', () {
    //   test("First Test", () {
    //     expect(detailscard, isInstanceOf<DetailsCardController>());
    //   });

    //   test("Set Value", () {
    //     expect(detailscard.value, equals(0));
    //     detailscard.increment();
    //     expect(detailscard.value, equals(1));
    //   });
  });
}
