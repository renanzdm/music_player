import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:musicplayer/app/shared/widgets/tab_bar/tab_bar_controller.dart';
import 'package:musicplayer/app/app_module.dart';

void main() {
  initModule(AppModule());
  // TabBarController tabbar;
  //
  setUp(() {
    //     tabbar = AppModule.to.get<TabBarController>();
  });

  group('TabBarController Test', () {
    //   test("First Test", () {
    //     expect(tabbar, isInstanceOf<TabBarController>());
    //   });

    //   test("Set Value", () {
    //     expect(tabbar.value, equals(0));
    //     tabbar.increment();
    //     expect(tabbar.value, equals(1));
    //   });
  });
}
