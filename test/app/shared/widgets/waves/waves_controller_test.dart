import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:musicplayer/app/shared/widgets/waves/waves_controller.dart';
import 'package:musicplayer/app/app_module.dart';

void main() {
  initModule(AppModule());
  // WavesController waves;
  //
  setUp(() {
    //     waves = AppModule.to.get<WavesController>();
  });

  group('WavesController Test', () {
    //   test("First Test", () {
    //     expect(waves, isInstanceOf<WavesController>());
    //   });

    //   test("Set Value", () {
    //     expect(waves.value, equals(0));
    //     waves.increment();
    //     expect(waves.value, equals(1));
    //   });
  });
}
