import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:musicplayer/app/shared/widgets/widgets_controller.dart';
import 'package:musicplayer/app/app_module.dart';

void main() {
  initModule(AppModule());
  // WidgetsController widgets;
  //
  setUp(() {
    //     widgets = AppModule.to.get<WidgetsController>();
  });

  group('WidgetsController Test', () {
    //   test("First Test", () {
    //     expect(widgets, isInstanceOf<WidgetsController>());
    //   });

    //   test("Set Value", () {
    //     expect(widgets.value, equals(0));
    //     widgets.increment();
    //     expect(widgets.value, equals(1));
    //   });
  });
}
